document.addEventListener('DOMContentLoaded',function(){document.querySelectorAll('.filter-bar').forEach(function(bar){var btns=bar.querySelectorAll('button[data-filter]'),grid=bar.nextElementSibling;if(!grid)return;var items=grid.querySelectorAll('[data-type],[data-category],[data-status]');btns.forEach(function(btn){btn.addEventListener('click',function(){btns.forEach(function(b){b.classList.remove('is-active')});btn.classList.add('is-active');var f=btn.getAttribute('data-filter');items.forEach(function(item){if(f==='all'){item.style.display=''}else{var match=(item.getAttribute('data-type')===f||item.getAttribute('data-category')===f||item.getAttribute('data-status')===f);item.style.display=match?'':'none'}})})})})});
